[pytest]
pythonpath = .
testpaths = tests
# Ruido esperado: tests construyen Application sin el extra opcional job-queue (apscheduler).
filterwarnings =
    ignore:.*No `JobQueue` set up.*:UserWarning
